"use client";
import {useState} from "react";import {Menu,X,ArrowRight} from "lucide-react";import {siteConfig} from "@/config/site";
export function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-white/10 bg-navy/90 text-white backdrop-blur-xl"><div className="container flex h-[76px] items-center justify-between"><a href="#top" className="text-xl font-bold tracking-tight"><span className="text-purple">Elva</span>Mind</a><nav aria-label="Primary navigation" className="hidden items-center gap-7 lg:flex">{siteConfig.navigation.map(n=><a key={n.href} href={n.href} className="text-[13px] font-medium text-white/70 transition hover:text-white">{n.label}</a>)}</nav><a href="#contact" className="btn btn-primary hidden py-3 lg:inline-flex">Book a Free Audit <ArrowRight size={15}/></a><button onClick={()=>setOpen(!open)} className="rounded-lg p-2 lg:hidden" aria-label="Toggle navigation" aria-expanded={open}>{open?<X/>:<Menu/>}</button></div>{open&&<nav aria-label="Mobile navigation" className="container border-t border-white/10 py-5 lg:hidden">{siteConfig.navigation.map(n=><a key={n.href} onClick={()=>setOpen(false)} href={n.href} className="block py-3 text-sm text-white/80">{n.label}</a>)}<a onClick={()=>setOpen(false)} href="#contact" className="btn btn-primary mt-3 w-full">Book a Free Automation Audit</a></nav>}</header>}
