"use client";import Image from "next/image";import {useState} from "react";import {siteConfig} from "@/config/site";
export function FounderImage(){const[failed,setFailed]=useState(false);const name:string=siteConfig.founder.name;const initials=name==="YOUR_NAME"?"YN":name.split(" ").map(x=>x[0]).join("").slice(0,2);return <div className="relative aspect-[4/5] overflow-hidden rounded-[28px] bg-gradient-to-br from-lavender to-purple/20">{failed?<div className="flex h-full items-center justify-center text-6xl font-bold text-purple">{initials}</div>:<Image src={siteConfig.founder.photo} alt={`Portrait of ${name}`} fill className="object-cover" sizes="(max-width: 768px) 100vw, 40vw" onError={()=>setFailed(true)}/>}</div>}
